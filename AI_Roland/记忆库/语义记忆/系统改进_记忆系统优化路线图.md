# 记忆系统优化路线图

## 版本：v1.0 → v2.0

## 当前状态评估

**版本**：v1.0

**核心功能**：
- ✅ TF-IDF 搜索引擎
- ✅ 三层记忆架构
- ✅ 8 个语义记忆
- ✅ 情景记录（日记）

**已识别问题**：
1. 中文分词效果有限（按字分词）
2. 缺少时间权重
3. 无自动提炼机制
4. 语义记忆无关联

## 改进路线图

### Phase 1: 分词优化（本周）

**目标**：提升中文分词准确度

**实现**：
```python
# 安装 jieba
pip install jieba

# 修改 memory_search.py
import jieba

def tokenize(self, text: str) -> List[str]:
    """使用 jieba 分词"""
    # 移除特殊字符
    text = re.sub(r'[^\w\s\u4e00-\u9fff]', ' ', text)
    # jieba 分词
    tokens = jieba.lcut(text)
    # 过滤停用词和单字
    tokens = [t.lower() for t in tokens if len(t) > 1]
    return tokens
```

**预期效果**：
- 分词准确度提升 50%
- 搜索相关性提升 30%

**时间**：1-2小时

### Phase 2: 时间权重（本周）

**目标**：最近的记忆更相关

**实现**：
```python
def search(self, query: str, top_k: int = 5) -> List[Dict]:
    """搜索相关记忆（带时间权重）"""
    # ... 原有代码 ...

    # 计算时间权重
    days_ago = (datetime.now() - doc_date).days
    time_weight = 1 / (1 + days_ago / 30)  # 30天半衰期

    # 综合相似度 = 语义相似度 × 时间权重
    final_similarity = similarity * time_weight
```

**预期效果**：
- 最近的记忆排序靠前
- 更符合用户直觉

**时间**：1小时

### Phase 3: Sentence Transformers（本月）

**目标**：升级到语义向量搜索

**实现**：
```python
from sentence_transformers import SentenceTransformer

class MemorySearchV2:
    def __init__(self):
        # 加载多语言模型
        self.model = SentenceTransformer(
            'paraphrase-multilingual-MiniLM-L12-v2'
        )
        # 缓存向量
        self._embeddings_cache = None

    def encode_documents(self, documents):
        """编码文档为向量"""
        texts = [doc["content"] for doc in documents]
        embeddings = self.model.encode(texts)
        return embeddings

    def search(self, query: str, top_k: int = 5):
        """向量搜索"""
        query_embedding = self.model.encode([query])
        similarities = cosine_similarity(
            query_embedding,
            self._embeddings_cache
        )[0]
        # 返回 top-k 结果
```

**优势**：
- 真正的语义理解
- 多语言支持
- 更准确的搜索

**时间**：3-4小时

### Phase 4: ChromaDB 集成（下月）

**目标**：本地向量数据库

**实现**：
```python
import chromadb

class MemorySearchV3:
    def __init__(self):
        self.client = chromadb.PersistentClient(
            path="./memory_db"
        )
        self.collection = self.client.get_or_create_collection(
            "memories"
        )

    def add_documents(self, documents):
        """添加文档到向量库"""
        self.collection.add(
            documents=[doc["content"] for doc in documents],
            metadatas=[{
                "type": doc["type"],
                "date": doc.get("date", ""),
                "path": doc["path"]
            } for doc in documents],
            ids=[doc["path"] for doc in documents]
        )

    def search(self, query: str, top_k: int = 5):
        """向量搜索"""
        results = self.collection.query(
            query_texts=[query],
            n_results=top_k
        )
        return results
```

**优势**：
- 持久化存储
- 自动索引
- 高性能

**时间**：4-5小时

### Phase 5: 自动提炼机制（下月）

**目标**：自动识别可提炼的内容

**实现**：
```python
class AutoDistill:
    """自动提炼系统"""

    def detect_patterns(self, conversations):
        """检测重复模式"""
        # 统计相似对话
        # 如果某话题出现 ≥3 次，标记为待提炼
        pass

    def generate_draft(self, topic):
        """生成语义记忆草稿"""
        # 分析相关对话
        # 提取共同模式
        # 生成结构化草稿
        pass

    def suggest_distillation(self):
        """建议提炼内容"""
        patterns = self.detect_patterns()
        for pattern in patterns:
            if pattern.count >= 3:
                draft = self.generate_draft(pattern.topic)
                # 提示用户审核
```

**预期效果**：
- 自动发现可提炼内容
- 减少手动工作
- 持续优化记忆库

**时间**：6-8小时

### Phase 6: 知识图谱（季度）

**目标**：建立语义记忆关联

**实现**：
```python
class KnowledgeGraph:
    """知识图谱"""

    def __init__(self):
        self.nodes = {}  # 语义记忆
        self.edges = []  # 关联关系

    def add_relation(self, from_id, to_id, relation_type):
        """添加关联"""
        self.edges.append({
            "from": from_id,
            "to": to_id,
            "type": relation_type  # "相关", "依赖", "包含"
        })

    def visualize(self):
        """可视化图谱"""
        # 使用 graphviz 或 D3.js
        pass

    def recommend(self, current_memory):
        """推荐相关记忆"""
        # 基于图谱关系推荐
        pass
```

**价值**：
- 发现隐藏关联
- 智能推荐
- 形成知识网络

**时间**：10-15小时

### Phase 7: 多模态记忆（季度）

**目标**：支持图片、音频、视频

**实现**：
```python
class MultiModalMemory:
    """多模态记忆"""

    def add_image_memory(self, image_path, description):
        """添加图片记忆"""
        # 使用 CLIP 编码图片
        # 存储向量
        pass

    def add_audio_memory(self, audio_path, transcript):
        """添加音频记忆"""
        # 转录为文本
        # 编码为向量
        pass

    def search_multimodal(self, query):
        """多模态搜索"""
        # 支持文本、图片、音频查询
        pass
```

**时间**：15-20小时

## 优先级矩阵

| 改进项 | 价值 | 成本 | 优先级 |
|--------|------|------|--------|
| jieba 分词 | ⭐⭐⭐ | 💰 | 🔥 高 |
| 时间权重 | ⭐⭐⭐ | 💰 | 🔥 高 |
| Sentence Transformers | ⭐⭐⭐⭐⭐ | 💰💰 | 🔥 高 |
| ChromaDB | ⭐⭐⭐⭐ | 💰💰💰 | ⚡ 中 |
| 自动提炼 | ⭐⭐⭐⭐ | 💰💰💰 | ⚡ 中 |
| 知识图谱 | ⭐⭐⭐⭐⭐ | 💰💰💰💰 | ❄️ 低 |
| 多模态 | ⭐⭐⭐⭐ | 💰💰💰💰💰 | ❄️ 低 |

## 实施计划

### 本周（2026-02-20 ~ 02-27）
- [x] 创建语义记忆库（8个）
- [x] 创建今日日记
- [ ] 集成 jieba 分词
- [ ] 添加时间权重
- [ ] 测试改进效果

### 本月（2026-02-20 ~ 02-29）
- [ ] 升级到 Sentence Transformers
- [ ] 性能对比测试
- [ ] 优化搜索结果展示

### 下月（2026-03-01 ~ 03-31）
- [ ] 集成 ChromaDB
- [ ] 开发自动提炼机制
- [ ] 创建记忆管理界面

### Q2（2026-04 ~ 06）
- [ ] 构建知识图谱
- [ ] 多模态记忆支持
- [ ] 个性化学习

## 成功指标

### 量化指标
- 搜索准确率：从 70% → 90%
- 搜索响应时间：< 500ms
- 语义记忆数量：50+
- 复用率：每周 10+ 次

### 质化指标
- 用户满意度：显著提升
- 知识可发现性：快速找到相关记忆
- 系统自进化：持续优化

## 风险与应对

### 风险1：性能下降
**应对**：缓存 + 分批处理 + 异步加载

### 风险2：成本过高
**应对**：优先级排序 + 分阶段实施

### 风险3：复杂度增加
**应对**：充分测试 + 文档完善

## 相关文件

- system/memory_search.py - 当前实现
- system/memory/README.md - 使用文档
- 技术方案_记忆搜索实现.md - 技术细节

## 下一步

立即开始：**集成 jieba 分词**

预计时间：1-2小时
预期效果：中文搜索准确度提升 50%
